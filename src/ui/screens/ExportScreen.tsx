/**
 * Screen 6: export. PLACEHOLDER (M2).
 *
 * Responsibility: download per-line summary CSV (export/summary-csv.ts),
 * segments CSV (export/segments-csv.ts), target check CSV, and the
 * self-contained HTML report (export/report.ts); PDF via the browser print
 * dialog. Downloads are Blob URLs created in the main thread.
 */
export function ExportScreen() {
  return (
    <section>
      <h2>Export</h2>
      <p>Placeholder.</p>
    </section>
  );
}
