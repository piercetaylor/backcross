/**
 * Screen 5: pairwise comparison. PLACEHOLDER (M2).
 *
 * Responsibility: pick two samples (candidate vs candidate, or candidate vs
 * recurrent parent), choose the mode ('informative' or 'all'), show
 * discordant counts overall and per chromosome, list discordant markers, and
 * render both lines stacked in the canvas renderer with discordant positions
 * marked.
 */
export function CompareScreen() {
  return (
    <section>
      <h2>Compare two lines</h2>
      <p>Placeholder.</p>
    </section>
  );
}
