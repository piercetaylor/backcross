/**
 * Screen 2: dataset summary and QC. PLACEHOLDER (M1).
 *
 * Responsibility: counts (markers, informative markers, samples by role,
 * chromosomes), parent polymorphism rate, per-line QC table with flags,
 * per-marker call-rate histogram, and the dataset-level flags from
 * computeQc. Lines flagged 'closer_to_donor' are highlighted.
 */
export function SummaryScreen() {
  return (
    <section>
      <h2>Dataset summary and QC</h2>
      <p>Placeholder.</p>
    </section>
  );
}
